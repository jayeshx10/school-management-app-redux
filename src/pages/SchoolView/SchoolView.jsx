import React from "react";
import { useDispatch, useSelector } from "react-redux";

export const SchoolView = () => {
  const dispatch = useDispatch();
  return <div>SchoolView</div>;
};

export default SchoolView;
